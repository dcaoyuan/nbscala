/** 
 * This is a Scala comment
 *
 * @author Caoyuan Deng
 */

package example

class ClassExample {
    var number: Int = 123;
    val str = "String";
    
    def getElement(index: Int) = {
        var targetLeft = 0;
        
        while (index > 0) {
            if (index > 0) {
                targetLeft += index;
            } else if (index < 0) {
                targetLeft += index;
            }
            targetLeft = index;
        }
        
        targetLeft; // line comment
    }
} 



